# frozen_string_literal: true

RSpec.describe Turbo::Inline::Editor do
  it "has a version number" do
    expect(Turbo::Inline::Editor::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
